// 文件路径: create/space/constants.ts (或你的实际路径)

/**
 * 代表“未分类”区域在 UI 拖拽交互中的唯一标识符。
 * 注意：这 *不是* 存储在 SpaceContent.categoryId 中的值。
 * 当内容未分类时，其 categoryId 属性会缺失或为 undefined。
 */
export const UNCATEGORIZED_ID = "__uncategorized_container__";
